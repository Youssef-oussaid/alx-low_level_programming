#include "main.h"

/**
 * _isdigit - function
 * @c: char
 * Return: 0 or 1
 */

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else if (c >= 48 && c <= 57)
	{
		return (0);
	}
	return (0);
}
