#include <stdio.h>

/**
 * main - entry point
 *
 * Description: a
 *
 * Return: always 0 (success)
 */

int main(void)
{
	char alph = 97;

	while (alph <= 122)
	{
		putchar(alph);
		alph++;
	}
	return (0);
}
