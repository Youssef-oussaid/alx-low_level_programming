#include <stdio.h>

/**
 * main - entry point
 *
 * Description: a
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int num = 0;

	while (num <= 9)
	{
		printf("%i\n", num);
		num++;
	}
	return (0);
}
